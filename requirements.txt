# Talos AI Dependencies
pydantic>=2.0
httpx>=0.27.0
python-dotenv>=1.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Optional: Temporal workflow orchestration
# Install with: pip install temporalio
# Enable with: TALOS_ENABLE_TEMPORAL=true
temporalio>=1.7.0
